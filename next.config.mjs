/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Si necesitas imágenes remotas, descomenta:
  // images: {
  //   remotePatterns: [
  //     { hostname: 'cdn.jsdelivr.net' }
  //   ]
  // }
}

export default nextConfig
